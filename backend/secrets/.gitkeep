# Ce dossier contient les clés RSA pour JWT RS256.
# Générer en dev:
#   openssl genrsa -out jwt_private.pem 4096
#   openssl rsa -in jwt_private.pem -pubout -out jwt_public.pem
#
# NE JAMAIS COMMITER LES .pem (cf. .gitignore).
# En prod: Cloud Secret Manager + Cloud KMS + rotation 90j.
